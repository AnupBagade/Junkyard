import React from 'react';
import JunkUserManagement from '../JunkUserManagement';

const EmployeeManagement = (props) => {
    return(
        <JunkUserManagement userType="Employee" searchUserType="junkuser_is_employee"/>
    )
}

export default EmployeeManagement;
